spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/resturant
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
#  security:
#    user:
#      name: gokul
#      password: password